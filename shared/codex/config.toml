model = "gpt-5.5"
model_reasoning_effort = "medium"

# Trusted project roots are added per-machine via `codex` itself; we don't
# bake host-specific absolute paths into the dotfiles repo. The previous
# /home/jay/code and /tmp/gocomet-spec-test-* entries were removed at
# repo bootstrap-mac.sh import.

[tui]
status_line = [
  "current-dir",
  "context-used",
  "five-hour-limit",
  "weekly-limit",
  "git-branch",
  "codex-version",
  "model-with-reasoning",
  "session-id",
]

[tui.model_availability_nux]
"gpt-5.5" = 4
